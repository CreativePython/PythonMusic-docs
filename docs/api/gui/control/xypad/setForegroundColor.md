# setForegroundColor()

Set the xypad's foreground [Color](../../color/index.md).  Identical to [setColor()](../../common/color/setColor.md)

## Parameters

Once an object `xypad` has been created, you can use the following functions:

```python
xypad.setForegroundColor()
```

```python
xypad.setForegroundColor(color)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `color` | `Color` | `None` | The new foreground color. If omitted, a color-selection dialog opens. |
