# getForegroundColor()

Return the xypad's foreground [Color](../../color/index.md).  Identical to [getColor()](../../common/color/getColor.md)

## Parameters

Once an object `xypad` has been created, you can use the following function:

```python
xypad.getForegroundColor()
```

## Returns

`return color`

| Value | Type | Description |
|---|---|---|
| color | `Color` | The current foreground color. |
