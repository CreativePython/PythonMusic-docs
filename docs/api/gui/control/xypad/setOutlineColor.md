# setOutlineColor()

Set the xypad's outline [Color](../../color/index.md).

## Parameters

Once an object `xypad` has been created, you can use the following functions:

```python
xypad.setOutlineColor()
```

```python
xypad.setOutlineColor(color)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `color` | `Color` | `None` | The new outline color. If omitted, a color-selection dialog opens. |
