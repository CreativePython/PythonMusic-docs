# getForegroundColor()

Return the push button's foreground [Color](../../color/index.md).  Identical to [getColor()](../../common/color/getColor.md)

## Parameters

Once an object `push` has been created, you can use the following function:

```python
push.getForegroundColor()
```

## Returns

`return color`

| Value | Type | Description |
|---|---|---|
| color | `Color` | The current foreground color. |
