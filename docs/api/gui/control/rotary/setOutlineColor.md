# setOutlineColor()

Set the rotary's outline [Color](../../color/index.md).

## Parameters

Once an object `rotary` has been created, you can use the following functions:

```python
rotary.setOutlineColor()
```

```python
rotary.setOutlineColor(color)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `color` | `Color` | `None` | The new outline color. If omitted, a color-selection dialog opens. |
