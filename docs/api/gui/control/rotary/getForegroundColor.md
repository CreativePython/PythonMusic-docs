# getForegroundColor()

Return the rotary's foreground [Color](../../color/index.md).  Identical to [getColor()](../../common/color/getColor.md)

## Parameters

Once an object `rotary` has been created, you can use the following function:

```python
rotary.getForegroundColor()
```

## Returns

`return color`

| Value | Type | Description |
|---|---|---|
| color | `Color` | The current foreground color. |
