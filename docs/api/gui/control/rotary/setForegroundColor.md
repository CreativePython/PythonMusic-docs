# setForegroundColor()

Set the rotary's foreground [Color](../../color/index.md).  Identical to [setColor()](../../common/color/setColor.md)

## Parameters

Once an object `rotary` has been created, you can use the following functions:

```python
rotary.setForegroundColor()
```

```python
rotary.setForegroundColor(color)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `color` | `Color` | `None` | The new foreground color. If omitted, a color-selection dialog opens. |
