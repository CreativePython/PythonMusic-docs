# setBackgroundColor()

Set the rotary's background [Color](../../color/index.md).

## Parameters

Once an object `rotary` has been created, you can use the following functions:

```python
rotary.setBackgroundColor()
```

```python
rotary.setBackgroundColor(color)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `color` | `Color` | `None` | The new background color. If omitted, a color-selection dialog opens. |
