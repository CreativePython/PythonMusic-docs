# onAction()

Register a function to call when the button is pressed.

## Parameters

Once an object `button` has been created, you can use the following function:

```python
button.onAction(action)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `action` | `function` | _required_ | The function to call each time the button is pressed; it receives no parameters. |
