# setOutlineColor()

Set the fader's outline [Color](../../color/index.md).

## Parameters

Once an object `fader` has been created, you can use the following functions:

```python
fader.setOutlineColor()
```

```python
fader.setOutlineColor(color)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `color` | `Color` | `None` | The new outline color. If omitted, a color-selection dialog opens. |
