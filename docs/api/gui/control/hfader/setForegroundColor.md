# setForegroundColor()

Set the fader's foreground [Color](../../color/index.md).  Identical to [setColor()](../../common/color/setColor.md)

## Parameters

Once an object `fader` has been created, you can use the following functions:

```python
fader.setForegroundColor()
```

```python
fader.setForegroundColor(color)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `color` | `Color` | `None` | The new foreground color. If omitted, a color-selection dialog opens. |
