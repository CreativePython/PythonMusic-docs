# setBackgroundColor()

Set the toggle's background [Color](../../color/index.md).

## Parameters

Once an object `toggle` has been created, you can use the following functions:

```python
toggle.setBackgroundColor()
```

```python
toggle.setBackgroundColor(color)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `color` | `Color` | `None` | The new background color. If omitted, a color-selection dialog opens. |
