# onAction()

Register a function to call when the toggle changes.

## Parameters

Once an object `toggle` has been created, you can use the following function:

```python
toggle.onAction(action)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `action` | `function` | _required_ | The function to call when the toggle changes; it receives the new value. |
