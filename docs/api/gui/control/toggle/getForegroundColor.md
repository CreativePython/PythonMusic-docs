# getForegroundColor()

Return the toggle's foreground [Color](../../color/index.md).  Identical to [getColor()](../../common/color/getColor.md)

## Parameters

Once an object `toggle` has been created, you can use the following function:

```python
toggle.getForegroundColor()
```

## Returns

`return color`

| Value | Type | Description |
|---|---|---|
| color | `Color` | The current foreground color. |
