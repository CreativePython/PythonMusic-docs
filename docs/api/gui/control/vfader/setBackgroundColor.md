# setBackgroundColor()

Set the fader's background [Color](../../color/index.md).

## Parameters

Once an object `fader` has been created, you can use the following functions:

```python
fader.setBackgroundColor()
```

```python
fader.setBackgroundColor(color)
```

| Parameter | Type | Default | Description |
|---|---|---|---|
| `color` | `Color` | `None` | The new background color. If omitted, a color-selection dialog opens. |
