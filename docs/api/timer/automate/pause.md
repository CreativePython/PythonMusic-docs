# pause()

Pause automation.

Every registered function stops being called until [resume()](resume.md); nothing is lost in the meantime.

## Parameters

`Automate.pause()` is a static utility. Call it on the `Automate` class itself, for example:

```python
Automate.pause()
```
