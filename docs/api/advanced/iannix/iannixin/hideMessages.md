# hideMessages()

Stop printing incoming OSC messages to the console.

## Parameters

Once an object `iannixin` has been created, you can use the following function:

```python
iannixin.hideMessages()
```
